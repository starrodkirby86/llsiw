/**
* anim_bg_layer
* The include for the rectangle entry logo.
*/

#include <pebble.h>
#pragma once

void anim_bg_layer_exit_anim(Window* window);
void anim_bg_layer_load(Window* window);
void anim_bg_layer_unload(Window* window);